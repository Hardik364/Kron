name: Production

# Triggers:
#   - Push to main   → builds production Docker images (requires manual approval)
#   - Push tag v*    → additionally builds signed musl binaries + GitHub Release
#
# Branch strategy:
#   feature/* → qa (QA environment, auto-deploy)
#   qa        → main (Production environment, manual approval)

on:
  push:
    branches: [main]
    tags: ["v*"]

env:
  CARGO_TERM_COLOR: always
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}/kron

jobs:
  # ── Always runs on push to main ──────────────────────────────────────────────
  # Build and push production Docker images
  build-images:
    name: Build Production Images (${{ matrix.service }})
    runs-on: ubuntu-latest
    environment: production # requires manual approval
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        service:
          - kron-collector
          - kron-normalizer
          - kron-stream
          - kron-alert
          - kron-auth
          - kron-query-api
          - kron-soar
          - kron-compliance
          - kron-ai
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_PREFIX }}/${{ matrix.service }}
          tags: |
            # On tag push: semver tags (v1.2.3 → 1.2.3, 1.2, latest)
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            # On branch push: sha tag (main-abc1234)
            type=sha,prefix=main-,format=short
            # Always tag latest on main
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: deploy/docker/${{ matrix.service }}.Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ── Only runs on tag v* ───────────────────────────────────────────────────────
  # Build statically-linked musl binaries for customer on-premise deployment
  build-binaries:
    name: Build ${{ matrix.binary }} (${{ matrix.target }})
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    environment: production
    strategy:
      fail-fast: false
      matrix:
        target:
          - x86_64-unknown-linux-musl
          - aarch64-unknown-linux-musl
        binary:
          - kron-collector
          - kron-normalizer
          - kron-stream
          - kron-alert
          - kron-auth
          - kron-query-api
          - kron-soar
          - kron-compliance
          - kron-ai
          - kron-ctl
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Install cross
        run: cargo install cross --git https://github.com/cross-rs/cross --locked

      - name: Build release binary
        run: cross build --release --target ${{ matrix.target }} -p ${{ matrix.binary }}

      - name: Install cosign
        uses: sigstore/cosign-installer@v3

      - name: Sign binary
        run: |
          cosign sign-blob \
            --key env://COSIGN_PRIVATE_KEY \
            --output-signature ${{ matrix.binary }}-${{ matrix.target }}.sig \
            target/${{ matrix.target }}/release/${{ matrix.binary }}
        env:
          COSIGN_PRIVATE_KEY: ${{ secrets.COSIGN_PRIVATE_KEY }}
          COSIGN_PASSWORD: ${{ secrets.COSIGN_PASSWORD }}

      - name: Upload binary and signature
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.binary }}-${{ matrix.target }}
          path: |
            target/${{ matrix.target }}/release/${{ matrix.binary }}
            ${{ matrix.binary }}-${{ matrix.target }}.sig

  # Create GitHub Release with all signed binaries attached
  create-release:
    name: Create GitHub Release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [build-binaries, build-images]
    runs-on: ubuntu-latest
    environment: production
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Create checksums
        run: |
          cd artifacts
          find . -type f ! -name "*.sig" ! -name "*.sha256" | sort | while read f; do
            sha256sum "$f" >> checksums.sha256
          done
          cat checksums.sha256

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/**/*
            artifacts/checksums.sha256
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
